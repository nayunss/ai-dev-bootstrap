# v0.2.4-pilot

상태: 발행 준비
발행 예정일: 2026-07-17

## 목적

`v0.2.3-pilot` 이후 검증된 공통 구현을 다음 downstream baseline으로 묶는다. 실제 downstream
Production readiness, live model trial과 독립 tester 운영은 이 release만으로 승인되지 않는다.

## 검증 완료 변경

- REQ-017: HANDOFF 상단 다음 작업과 하단 남은 작업의 안정적 ID 일치 및 완료 작업 잔존 차단
- REQ-026·033: Gitleaks·Opengrep project-local offline installer와 npm·pnpm·Yarn·Maven·Gradle·
  Python application별 dependency bootstrap
- REQ-040: 법률·retention owner, multi-instance limiter와 provider restore evidence schema v2 hard gate
- REQ-041: bounded patch synthetic pilot과 실제 호출 전 offline trial-plan gate
- REQ-042: Codex·Claude Code·GitHub Copilot adapter, canonical YAML lock·manifest/core materializer,
  transaction upgrade·rollback·finalize automation
- REQ-043: network-none Podman ScanCode hosted conditional gate와 sanitized 14일 artifact
- REQ-046: 다중 tester 결과 schema·validator·aggregator synthetic Eval

## Migration

1. 현재 downstream의 release tag·commit·archive checksum과 local override를 기록한다.
2. `v0.2.4-pilot` archive checksum과 release manifest를 검증한다.
3. 격리 target에서 core upgrade `preview`로 create·update·delete·preserve diff를 검토한다.
4. project별 security tool·dependency profile과 기존 파일 충돌을 확인한다.
5. 공통·project regression과 `scripts/validate <target>`을 통과한 뒤 별도 승인으로 apply한다.
6. 검증 중에는 rollback record를 유지하고 승인 완료 후 finalize한다.

실제 downstream apply, dependency network, lifecycle script, hook, browser binary와 Production 변경은
각각 별도 Human-in-the-loop 승인 대상이다.

## 검증

- 전체 deterministic fixture suite와 downstream validation
- core upgrade create·update·delete·preserve, rollback/finalize와 transaction failure restore
- security tool checksum·collision·drift 및 dependency 6종 clean install·보존 uninstall
- HANDOFF·CodeSight stale, Gitleaks·Opengrep와 hosted `security`·`license-provenance`
- tracked archive 생성·checksum 및 게시 asset 재다운로드 검증

## Rollback

- downstream 검증 중에는 `scripts/upgrade-core.mjs rollback`으로 이전 manifest/source와 canonical lock을
  복구한다.
- finalize 이후에는 개별 파일을 삭제하지 않고 downstream upgrade commit 전체를 revert한다.
- application code, database migration, provider restore와 Production deployment rollback은 별도 경계다.
- 실패하면 이전 `v0.2.3-pilot` tag와 게시 checksum으로 돌아간다.

## Artifact

- tag commit: 발행 후 기록
- archive SHA-256: 발행 후 기록
- release: <https://github.com/nayunss/ai-dev-bootstrap/releases/tag/v0.2.4-pilot>
- 게시 asset 재다운로드 checksum: 발행 후 검증
