{
  "openapi": "3.1.0",
  "info": {
    "title": "Synthetic Items API",
    "version": "2.0.0"
  },
  "paths": {
    "/items": {
      "get": {
        "operationId": "list_items",
        "parameters": [
          {
            "name": "tenant",
            "in": "query",
            "required": true,
            "schema": { "type": "string" }
          }
        ],
        "responses": {
          "204": { "description": "No content" }
        }
      }
    }
  },
  "components": { "schemas": {} }
}
